{
  "name": "braid",
  "version": "0.2.0",
  "private": true,
  "description": "Continuous architecture evolution for growing codebases.",
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -b --force --pretty false",
    "typecheck": "tsc -b --pretty false",
    "lint": "eslint . --max-warnings=0",
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "prettier --write .",
    "braid": "node apps/cli/dist/index.js",
    "analyze:example": "pnpm build && node apps/cli/dist/index.js analyze examples/bloated-saas --no-save",
    "propose:example": "pnpm build && node apps/cli/dist/index.js propose examples/bloated-saas --no-save",
    "benchmark:list": "pnpm build && node packages/benchmark/dist/cli/index.js list",
    "benchmark:smoke": "pnpm build && node packages/benchmark/dist/cli/index.js run --suite phase-2-core --smoke",
    "benchmark:run": "pnpm build && node packages/benchmark/dist/cli/index.js run --suite phase-2-core",
    "benchmark:compare": "pnpm build && node packages/benchmark/dist/cli/index.js compare --suite static-comparison",
    "benchmark:report": "node packages/benchmark/dist/cli/index.js report"
  },
  "devDependencies": {
    "@eslint/js": "^9.31.0",
    "@types/node": "^22.15.0",
    "eslint": "^9.31.0",
    "prettier": "^3.6.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.36.0",
    "vitest": "^3.2.4"
  }
}

description = "Show redacted Braid Growth Mode and native Copilot CLI adapter status"
prompt = "Run `braid growth status --host copilot --session native-manual-copilot` and report the result. Do not expose account identifiers, tokens, request IDs, session secrets, transcripts, or absolute home paths. Make no changes."

---
name: setup
description: Check the Braid CLI, project initialization, Growth Mode, and native Codex adapter without changing project configuration.
---

# Braid setup

Run `braid --version`. If the command is missing, report this installation
command but do not run it:

```sh
curl -fsSL https://raw.githubusercontent.com/ting10688/Braid/main/install.sh | sh
```

Otherwise run `braid growth setup --host codex`. Report its one exact next
command. Do not install software, run `braid init`, enable Growth Mode, edit
hooks, or change trust without the user's explicit approval.
